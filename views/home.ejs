<section>
        <h1>Sociade Home Page with ejs </h1>
        <% if(locals.user){%>
        <form id="newpostform" action="/post/createpost" method="POST">
                <!-- <p>id: <%= user.id%></p> -->
                <textarea name="content" cols="30" rows="5" placeholder="write here"></textarea>
                <input type="submit" value="Post">
        </form>
        <%}%>
        <div id="post-list">
                
                <ul>
                    <% for(i of postforhome){%>
                    <%- include('_post') -%>
                    <% } %>
                </ul>
        </div>
</section>
<section>
        <%if(locals.user){%>
                <% for( u of userforhome){%>
                        <p>
                                <a href="/users/profile/<%=u.id%>"><%=u.name%></a>
                        </p>
                <%}%>
        <%}%>
</section>
       
     <script src="/js/homepost.js"></script>